kind = "web"
previewPath = "/"
title = "Business Development Agent"
version = "1.0.0"
id = "artifacts/bda"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23573

[services.development]
run = "pnpm --filter @workspace/bda run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/bda", "run", "build" ]
publicDir = "artifacts/bda/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23573"
BASE_PATH = "/"
